Update the Contact page (`src/pages/ContactPage.tsx`) to show both phone numbers labelled by language anywhere a single number is currently displayed.

1. **"Call Us" info card** (top of contact page): replace the single `+421 905 616 418` value with two stacked lines — `EN +421 905 616 418` (tel:+421905616418) and `SK +421 917 600 610` (tel:+421917600610). Each number stays clickable.

2. **Office cards** (Košice + Bratislava): replace the single phone line with the same two labelled, clickable numbers.

No translation strings or other pages need changes (footer and contact form were already updated previously).